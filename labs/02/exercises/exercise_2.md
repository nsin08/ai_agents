# Exercise 2: Add a new custom tool

Task:
- Add a new tool to `labs/02/src/custom_tools.py` (e.g., `TextStatsTool`).
- Register it in `build_registry()`.

Hint:
<!-- Hint: mirror the structure of CalculatorTool. -->
