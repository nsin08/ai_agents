# Exercise 3: Handle tool execution errors

Task:
- Update `run_tool_sequence()` to handle tool failures gracefully.
- If a tool fails, capture the error and continue.

Hint:
<!-- Hint: check `result.success` and use `result.error`. -->
