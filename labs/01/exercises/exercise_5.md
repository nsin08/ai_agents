# Exercise 5: Use ChromaDB for retrieval

Task:
- Replace the mock vector store with ChromaDB.
- Persist embeddings locally in a `labs/01/data/chroma/` directory.

Hint:
<!-- Hint: use `chromadb.PersistentClient(path=...)` and a collection named `rag_docs`. -->
