# Exercise 4: Use Ollama for generation

Task:
- Replace `MockProvider` in `labs/01/src/rag_agent.py` with `OllamaProvider`.
- Add env-driven configuration: `OLLAMA_BASE_URL`, `OLLAMA_MODEL`, `OLLAMA_TIMEOUT`.

Hint:
<!-- Hint: import OllamaProvider and read env vars from os.getenv. -->
